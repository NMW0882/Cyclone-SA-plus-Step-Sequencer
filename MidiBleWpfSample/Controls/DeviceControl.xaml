<UserControl x:Class="MidiBleWpfSample.Controls.DeviceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MidiBleWpfSample.Controls"
             Height="300" Width="150">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- デバイス名と接続状態 -->
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="{Binding DeviceName}" FontWeight="Bold" FontSize="14"/>
            <Ellipse Width="10" Height="10" Margin="5,0,0,0"
                     Fill="{Binding IsConnected, Converter={StaticResource BoolToBrushConverter}}"/>
        </StackPanel>

        <!-- Range スライダー -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,5,0,0">
            <TextBlock Text="Range" FontWeight="Bold"/>
            <Slider Minimum="0" Maximum="127" Value="{Binding RangeValue}" Width="130"/>
        </StackPanel>

        <!-- Gain スライダー -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,5,0,0">
            <TextBlock Text="Gain" FontWeight="Bold"/>
            <Slider Minimum="0" Maximum="1" Value="{Binding GainValue}" Width="130"/>
        </StackPanel>

        <!-- 反転＆ディレイ設定 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5,0,0">
            <CheckBox Content="Invert"
                      IsChecked="{Binding Invert}"
                      Margin="0,0,10,0"/>
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Delay (ms)" FontWeight="Bold"/>
                <TextBox Width="50" Text="{Binding DelayValue}"/>
            </StackPanel>
        </StackPanel>


    </Grid>
</UserControl>
