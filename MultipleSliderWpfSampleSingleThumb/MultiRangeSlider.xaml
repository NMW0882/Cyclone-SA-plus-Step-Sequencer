<UserControl x:Class="MultipleSliderWpfSample.MultiRangeSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400"
             x:Name="Root">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SliderStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <!-- Interactive Layer -->
        <Canvas x:Name="SliderCanvas" Margin="24,0" Background="Transparent" MinHeight="30">
            <!-- Track -->
            <Border Height="2" Background="#DDDDDD" CornerRadius="1" Canvas.Top="21" Width="{Binding ActualWidth, ElementName=SliderCanvas}"/>
            
            <!-- Range Highlight -->
            <Rectangle x:Name="RangeHighlight" Height="2" Fill="#007ACC" Canvas.Top="21"/>
            
            <!-- Labels -->
            <TextBlock x:Name="LeftLabel" Text="0" FontSize="10" Foreground="#333333" Canvas.Top="0"/>
            <TextBlock x:Name="RightLabel" Text="0" FontSize="10" Foreground="#333333" Canvas.Top="0"/>
            <TextBlock x:Name="MainLabel" Text="0" FontSize="10" Foreground="#333333" Canvas.Top="0"/>

            <!-- Left Ear (Range Start) -->
            <Thumb x:Name="LeftThumb" DragDelta="LeftThumb_DragDelta" Background="#555555" Style="{StaticResource LeftThumbStyle}" Height="14" Width="10" Canvas.Top="15"/>
            
            <!-- Right Ear (Range End) -->
            <Thumb x:Name="RightThumb" DragDelta="RightThumb_DragDelta" Background="#555555" Style="{StaticResource RightThumbStyle}" Height="14" Width="10" Canvas.Top="15"/>
            
            <!-- Main Thumb (Value) -->
            <Thumb x:Name="MainThumb" DragDelta="MainThumb_DragDelta" Background="#007ACC" Style="{StaticResource SimpleThumbStyle}" Width="14" Height="14" Canvas.Top="15"/>
        </Canvas>
    </Grid>
</UserControl>
